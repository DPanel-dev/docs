# 容器端口访问

## 映射端口

创建容器时映射容器内部的端口后，可通过 `http://ip:port` 的形式进行访问。
默认情况下，面板会根据当前浏览器地址或当前服务端的连接参数自动获取 IP 地址。

如需配置 IP 地址或指定为域名，通过[配置服务端访问地址](/manual/system-env#server-url)修改。

## 绑定域名

为容器绑定域名访问请查看[域名转发](/manual/container-domain)。


